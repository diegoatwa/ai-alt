/node_modules
/static
*temp.md


# ai-alt (prevent sensitive data leak) 
.env


# ai-alt (prevent sensitive data leak) 
.env


# ai-alt (prevent sensitive data leak) 
.env


# ai-alt (prevent sensitive data leak) 
.env
